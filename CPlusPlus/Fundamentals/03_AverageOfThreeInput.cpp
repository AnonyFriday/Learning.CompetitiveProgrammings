#include <bits/stdc++.h>

using namespace std;

int main() {
  ios_base::sync_with_stdio(0);

  // User Input Data
  int a, b, c;
  cin >> a >> b >> c;

  // Processing
  float result = (a + b + c) / 3.0;

  // Output
  cout << "Average Marks: " << result << endl;

  return 0;
}